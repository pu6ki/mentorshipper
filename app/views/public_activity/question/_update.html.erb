<li class="dropdown-item">
  <small class="text-muted"><%= a.created_at.strftime('%-d %B %Y %H:%M:%S') %></small><br/>
  <strong><%= activity.owner.full_name %></strong>
  <% if a.trackable %>
    edited question <%= link_to a.trackable.title, question_path(a.trackable) %>
  <% else %>
    edited a question that has been deleted
  <% end %>
</li>