<li class="dropdown-item">
  <small class="text-muted"><%= a.created_at.strftime('%-d %B %Y %H:%M:%S') %></small><br/>
  <strong><%= activity.owner %></strong> 
  answered question 
  <% if a.trackable %>
    <%= link_to a.trackable.question.title, question_path(a.trackable.question) %>
  <% else %>
    that has been deleted
  <% end %>
</li>